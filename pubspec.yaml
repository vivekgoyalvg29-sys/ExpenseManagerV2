name: expense_manager
description: Personal Expense Manager

publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  sqflite: ^2.3.3
  path: ^1.9.0
  intl: ^0.18.1
  fl_chart: ^0.66.2
  home_widget: ^0.5.0


flutter:
  uses-material-design: true
